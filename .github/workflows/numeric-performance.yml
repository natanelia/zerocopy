name: Numeric performance proof
on:
  push:
    branches: [agent/perf-numeric-range]
  pull_request:
    paths: ['numeric*', 'proofs/numeric*', 'scripts/build-wasm.mjs', 'scripts/build-browser.ts', '.github/workflows/numeric-performance.yml']
permissions:
  contents: read
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: '1.4.2'
      - run: bun install
      - run: bun run build:wasm && bun run build:browser && bun run build:types
      - run: bun run typecheck && bun run typecheck:redux && bun run typecheck:values
      - run: bun run test
      - run: node proofs/numeric-worker.mjs
      - run: PERF_GATE=1 node proofs/numeric-benchmark.mjs
      - run: bun proofs/numeric-benchmark.mjs
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: numeric-proof-${{ runner.arch }}
          path: proofs/results/numeric-*.json
          if-no-files-found: error
          retention-days: 30
