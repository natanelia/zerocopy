# Redux integration

Use the [`zerocopy/redux` guide](docs/redux.md) for store setup, selectors,
DevTools, persistence, workers, and memory ownership.
Read the [compatibility notes](docs/redux-compatibility.md) before enabling
portable DevTools serialization. Use the checks on the exact PR commit for test results.
